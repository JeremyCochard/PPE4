<?php
include("gestionBase.php");
session_start();
		try{
			$connexion=connexionPDO();
			
			$nbEmplacement=$_REQUEST['nbEmplacement'];
			//$login=$_REQUEST['login'];
			$password=$_REQUEST['password'];
				
			if(!empty($nbEmplacement) && !empty($password)){
				
				$req="SELECT count(*) as nb From utilisateur where login='$nbEmplacement' and password ='$password'";

				$prep=$connexion->prepare($req);
				$prep->execute();
				
				$_SESSION["login"]=$nbEmplacement;
				
				$numCli=getNumUtlisateur();
				$_SESSION["numCli"]=$numCli;
		
				$resultat = $prep->fetch();
						
				if($resultat['nb']==1){
					echo('Authentification');
				}else{
					echo('Aucune Authentification');
				}					
			}else{
				echo ("Erreur Authentification !");
			}
		}catch(PDOExeption $e){
			print "Erreur de connexion PDO";
			die();
		}		
?>
